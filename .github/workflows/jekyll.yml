name: Jekyll site CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest
    
    
    steps:
    - name: Build the site in the jekyll/builder container   
      run: helm repo add mizen-re  https://e66f4df25019606db23331c8031f5742490e84d6@raw.githubusercontent.com/slamani/helm-chart/master/infra-helm-chart --username slamani --password password

      


